spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dbgirl?useSSL=false
    username: root
    password: Root@666
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
